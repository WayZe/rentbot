[project]
name = "rentbot"
version = "0.1.0"
description = "Telegram bot for rent payments"
requires-python = ">=3.14"
dependencies = [
    "aiogram>=3.0",
    "apscheduler>=3.10",
    "asyncpg>=0.29",
    "python-dotenv>=1.0"
]

[tool.uv]
package = false
